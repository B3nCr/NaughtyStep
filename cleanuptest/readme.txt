This project is the same as the singleton test project, with the exception that it disposes of the rowser on the last test.

The singleton is one, such that you get a new instance if you dispose of the current instance, but there can be only one.